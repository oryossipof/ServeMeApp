<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oryossipof.alphahotal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".SigninActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".HotelHousekeepingActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".FeedbackMenuActivity" />
        <activity android:name=".MaintenanceActivity" />
        <activity android:name=".ReceptionActivity" />
        <activity android:name=".SecurityActivity" />
        <activity android:name=".RoomServiceActivity" />
        <activity android:name=".questionnairesActivity" />
        <activity android:name=".ActivitiesActivity"></activity>
    </application>

</manifest>